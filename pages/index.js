import { Layout } from '@components';
import { Hero, Promotional } from '@sections';

const Home = () => {
  return (
    <Layout>
      <Hero />
      <Promotional />
    </Layout>
  );
};

export default Home;
